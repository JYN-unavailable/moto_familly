ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=FR

# Réseau MotoFamily — généré par install.sh
network={
    ssid="MotoFamily"
    psk="MotoFamily2024"
    key_mgmt=WPA-PSK
    priority=10
}
